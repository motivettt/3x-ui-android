# Инструкция по загрузке в GitHub

## 📤 Шаги для загрузки проекта

### 1. Инициализация Git (если еще не сделано)

```bash
git init
git add .
git commit -m "Initial commit: 3X-UI Android Client"
```

### 2. Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите "+" → "New repository"
3. Назовите репозиторий (например: `3x-ui-android`)
4. Выберите **Public** (или Private, если хотите)
5. **НЕ** создавайте README, .gitignore или LICENSE (они уже есть!)
6. Нажмите "Create repository"

### 3. Подключение к GitHub

```bash
git remote add origin https://github.com/ВАШ_USERNAME/3x-ui-android.git
git branch -M main
git push -u origin main
```

Замените `ВАШ_USERNAME` на ваш GitHub username.

### 4. Загрузка файлов

```bash
git push -u origin main
```

## 📦 Создание первого релиза с APK

### Способ 1: Через веб-интерфейс GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **"Releases"** справа
3. Нажмите **"Create a new release"**
4. Заполните:
   - **Tag version:** `v1.0.0`
   - **Release title:** `v1.0.0 - First Release`
   - **Description:** 
     ```
     Первый релиз 3X-UI Android Client
     
     ## Особенности
     - Современный дизайн с анимациями
     - Функционал Ping
     - Полная поддержка WebView
     ```
5. Перетащите файл `app/build/outputs/apk/debug/app-debug.apk` в раздел "Attach binaries"
6. Нажмите **"Publish release"**

### Способ 2: Через командную строку

```bash
# Соберите APK
./gradlew assembleDebug

# Создайте тег
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправьте тег на GitHub
git push origin v1.0.0
```

Затем создайте релиз через веб-интерфейс GitHub и прикрепите APK файл.

## ✅ Проверка безопасности

Убедитесь что в репозитории **НЕТ**:
- ❌ `local.properties` (уже в .gitignore)
- ❌ `.gradle/` директории (уже в .gitignore)
- ❌ `build/` директории (уже в .gitignore)
- ❌ Файлов с ключами или паролями

## 🔒 Защита аккаунта

Ваш аккаунт **НЕ будет забанен**, потому что:

1. ✅ `.gitignore` правильно настроен
2. ✅ `local.properties` исключен (содержит пути к SDK)
3. ✅ Секретные ключи не включены в проект
4. ✅ Проект использует стандартные Android зависимости
5. ✅ Нет нарушающего контента

## 📝 Обновление README

После создания репозитория обновите в README.md:
- Замените `ВАШ_USERNAME` на ваш GitHub username
- Добавьте скриншоты приложения (если есть)

## 🚀 Автоматическая сборка APK

После первого релиза, при создании тега `v1.0.1`, `v1.0.2` и т.д., GitHub Actions автоматически соберет и загрузит APK в релиз.

Просто создайте тег:
```bash
git tag -a v1.0.1 -m "Release version 1.0.1"
git push origin v1.0.1
```

## 💡 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть что будет загружено
git ls-files

# Убедиться что local.properties не включен
git check-ignore -v local.properties
```

---

**Готово!** Ваш проект теперь на GitHub с красивым README и готовым к использованию.

